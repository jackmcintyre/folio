{
  "name": "folio",
  "version": "0.1.0",
  "private": true,
  "description": "Folio — Claude-to-filesystem relay (MCP Streamable HTTP + Cloudflare Workers + Node puller)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "npm run typecheck --prefix relay && npm run typecheck --prefix puller"
  },
  "devDependencies": {
    "@iarna/toml": "^2.2.5",
    "vitest": "^3.2.4"
  }
}
