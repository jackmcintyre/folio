{
  "name": "folio-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Folio relay — Cloudflare Worker: MCP transport, OAuth edge, Durable Object broker",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "agents": "0.16.2",
    "@cloudflare/workers-oauth-provider": "0.8.1",
    "@modelcontextprotocol/sdk": "^1.29"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260623.0",
    "typescript": "5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.103.0"
  }
}
